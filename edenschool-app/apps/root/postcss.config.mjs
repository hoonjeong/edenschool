// Tailwind v4 — 독서교육원(/reading) 세그먼트 전용 스타일 처리.
// Tailwind 지시자(@import "tailwindcss")가 없는 CSS(Bootstrap 등)는 그대로 통과한다.
const config = {
  plugins: {
    "@tailwindcss/postcss": {},
  },
};

export default config;
