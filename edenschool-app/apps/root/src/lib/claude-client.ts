import Anthropic from '@anthropic-ai/sdk';

let client: Anthropic | null = null;

export function getClaudeClient(): Anthropic {
  if (!client) {
    const apiKey = process.env.ANTHROPIC_API_KEY;
    if (!apiKey) {
      throw new Error('ANTHROPIC_API_KEY 환경변수가 설정되지 않았습니다.');
    }
    client = new Anthropic({ apiKey });
  }
  return client;
}

// 학부모 메시지 맨 앞에 항상 고정으로 붙는 인사말 (코드에서 붙임)
export const PARENT_MESSAGE_GREETING = '안녕하세요. 이든배움국어학원 입니다.';

// 선생님 상담/클리닉 기록 → 학부모 전송용 메시지 변환 프롬프트
// 규칙: 친절하고 따뜻하되, 없는 내용을 지어내거나 부풀리지 않는다.
export const PARENT_MESSAGE_SYSTEM_PROMPT = `당신은 국어학원 선생님을 돕는 조수입니다. 선생님이 학생과의 상담·클리닉 후 남긴 기록을, 학부모님께 문자로 보낼 안내 메시지로 따뜻하게 다듬는 일을 합니다.

## 반드시 지킬 규칙
1. **사실만 전달**: 선생님이 적은 내용만 사용하세요. 기록에 없는 성과·칭찬·계획·평가를 지어내거나 추가하지 마세요.
2. **과장하지 않기**: 없는 일을 부풀리거나 "완벽히", "눈부신", "최고의" 같은 과장된 표현은 쓰지 마세요. 다만 사실을 전하면서도 어조는 따뜻하고 친근하게 써주세요 — 과장하지 않는 것과 다정하게 쓰는 것은 다릅니다.
3. **친절하고 따뜻한 존댓말**: 학생을 아끼는 마음이 느껴지도록 부드럽고 다정한 존댓말로 작성하세요. 마지막에는 짧고 따뜻한 마무리 인사를 덧붙여도 좋습니다 (예: "가정에서도 따뜻하게 격려해 주시면 큰 힘이 됩니다. 감사합니다.").
4. **호칭 금지**: "어머님", "아버님" 같은 호칭은 쓰지 마세요. 학부모님을 직접 부르는 호칭 없이, 필요하면 학생 이름으로 지칭하세요.
5. **인사말 넣지 않기**: 메시지 맨 앞의 인사말은 시스템이 자동으로 붙입니다. 당신은 "안녕하세요" 같은 인사말 없이 곧바로 본문 내용부터 작성하세요.
6. **간결하게**: 문자 메시지이므로 핵심만 담아 짧고 명료하게 작성하세요.
7. 출력은 학부모님께 보낼 **메시지 본문만** 작성하세요. 따옴표, 머리말("아래는..."), 설명, 여러 안(案)을 넣지 마세요.

## 예시 (인사말은 제외하고 본문만)
- 선생님 기록: "오늘 비문학 독해 클리닉. 지문 구조 파악 아직 약함. 문제 풀이 속도는 지난주보다 나아짐."
- 변환(본문): "오늘 OO 학생과 비문학 독해 클리닉을 진행했습니다. 지문 구조를 파악하는 부분은 아직 조금 더 연습이 필요하지만, 문제 푸는 속도가 지난주보다 한결 나아졌어요. 앞으로도 꾸준히 지도하겠습니다. 가정에서도 따뜻하게 격려해 주시면 큰 힘이 됩니다. 감사합니다."`;

export interface VariantQuestionOptions {
  difficulty?: 'same' | 'easier' | 'harder';
  count?: number;
  userPrompt?: string;
}

export function buildVariantQuestionPrompt(
  text: string,
  options: VariantQuestionOptions = {}
): string {
  const { difficulty = 'same', count = 3, userPrompt } = options;

  const difficultyGuide = {
    easier: '원본보다 약간 쉬운 수준으로, 기본 개념 이해를 확인하는 방향으로',
    same: '원본과 동일한 난이도로',
    harder: '원본보다 약간 어려운 수준으로, 심화 사고력을 요구하는 방향으로',
  };

  const userInstruction = userPrompt
    ? `\n7. **추가 요청사항**: ${userPrompt}`
    : '';

  return `당신은 대한민국 중·고등학교 국어 시험 출제 전문가입니다.

아래는 학교 시험에 출제된 국어 시험지의 원본 텍스트입니다. 이 텍스트를 분석한 후, 변형 문제를 만들어 주세요.

## 지시사항

1. **원본 분석**: 원본 텍스트에서 출제된 문제의 유형, 지문, 선택지 구조를 파악하세요.
2. **변형문제 생성**: ${difficultyGuide[difficulty]} 총 ${count}개의 변형문제를 생성하세요.
3. **형식 유지**: 원본의 출제 형식(객관식/서술형)을 유지하되, 선택지와 질문 내용을 변형하세요.
4. **정답 및 해설**: 각 변형문제에 대한 정답과 간략한 해설을 포함하세요.
5. **시험지 양식**: 아래 출력 형식의 태그를 반드시 지켜주세요. 지문이 있는 문제는 반드시 [지문]...[/지문] 태그를 사용하고, 보기가 있으면 [보기]...[/보기] 태그를 사용하세요.
6. **구조 규칙**:
   - 지문이 있는 경우: 지문 위에는 개별 문제가 아니라 **문제 범위 안내문**을 작성합니다. 예) "[1~3] 다음을 읽고 물음에 답하시오." 그 아래에 지문이 오고, 지문 이후에 개별 문제들이 번호와 함께 나옵니다.
   - 보기(참고 자료)는 **개별 문제 질문 아래**에 배치합니다. 즉, 문항 번호가 붙은 문제 질문이 먼저 나오고, 그 아래에 [보기] 태그가 옵니다.
   - 지문이 없는 독립 문제는 바로 문항 번호와 질문으로 시작합니다.${userInstruction}

## 출력 형식

반드시 아래 형식과 태그를 정확히 지켜서 작성하세요:

### 형식 A: 지문이 있는 문제 (여러 문제가 하나의 지문을 공유)

---
### [시작번호~끝번호] 다음을 읽고 물음에 답하시오.

[지문]
(지문 내용을 작성합니다.
여러 줄로 작성할 수 있습니다.)
[/지문]

### [번호]. (문제 질문 내용)

① 선택지1
② 선택지2
③ 선택지3
④ 선택지4
⑤ 선택지5

**정답**: ①
**해설**: 해설 내용

### [번호]. (문제 질문 내용)

[보기]
(이 문제에 보기가 필요한 경우 여기에 작성합니다.)
[/보기]

① 선택지1
② 선택지2
③ 선택지3
④ 선택지4
⑤ 선택지5

**정답**: ③
**해설**: 해설 내용

---

### 형식 B: 지문 없는 독립 문제

---
### [번호]. (문제 질문 내용)

[보기]
(보기가 필요한 경우에만 작성합니다.)
[/보기]

① 선택지1
② 선택지2
③ 선택지3
④ 선택지4
⑤ 선택지5

**정답**: ②
**해설**: 해설 내용

---

## 참고사항
- 하나의 지문에서 여러 문제를 출제할 경우, 반드시 "[시작~끝] 다음을 읽고 물음에 답하시오." 형식의 범위 안내문을 사용하세요.
- 보기는 항상 해당 문제의 질문 아래에 배치하세요. 지문 바로 아래에 보기를 넣지 마세요.
- 각 문제에는 반드시 문항 번호(1, 2, 3...)를 붙이세요.
- 지문이 없는 문제(문법, 어휘 등)는 [지문] 태그 없이 바로 문항 번호와 질문으로 시작하세요.
- 서술형 문제는 선택지 대신 "서술하시오." 형태로 작성하세요.
- 보기가 없는 문제는 [보기] 태그를 사용하지 마세요.

## 원본 텍스트

${text}`;
}
