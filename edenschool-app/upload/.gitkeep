# 레거시 업로드 폴더
# 구 시스템과 동일 구조로 파일을 디스크에 저장한다(DB에는 파일명만):
#   upload/         → 게시물 첨부파일 (pdf, hwp, doc 등)
#   upload/image/   → 본문 이미지 (jpg, png 등) — image-view.html?id= 가 여기서 읽음
# 실제 파일은 깃에 커밋하지 않는다(서버에서 구 시스템 upload 폴더를 그대로 복사).
